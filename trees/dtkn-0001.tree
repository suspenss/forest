\date{2024-09-26}
\title{Memos}
\import{common}

\memo{
  \p{
    \ul{
      \li{若 #{f(x)} 是周期函数，则 #{g(f(x))} 也是周期函数。}
      \li{若 #{f(x)} 以 #{T} 为周期，则 #{f(a x + b)} 以 #{\frac{T}{|a|}} 为周期。}
      \li{若 #{f(x)} 可导，则其与 #{f'(x)} 周期相同。}
    }
  }
}
